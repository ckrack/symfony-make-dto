{% extends 'base.html.twig' %}

{% block title %}Foobar{% endblock %}

{% block body %}
    <h1>Foobar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ foobar.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ foobar.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('foobar_index') }}">back to list</a>

    <a href="{{ path('foobar_edit', {'id': foobar.id}) }}">edit</a>

    {{ include('foobar/_delete_form.html.twig') }}
{% endblock %}
